# Safe to commit — credentials to reach Argus, not secret values.
ARGUS_BUCKET_ID=
ARGUS_BUCKET_TOKEN=

# Optional: override a bucket-mapped key locally
# DATABASE_URL=postgresql://localhost:5432/myapp_dev
